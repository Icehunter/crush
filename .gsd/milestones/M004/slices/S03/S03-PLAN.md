# S03: Git Worktree Isolation

**Goal:** Implement a standalone WorktreeManager in `internal/auto/worktree.go` that creates, merges, and removes git worktrees for milestone isolation, with a `worktree_mode` config field in Options. All operations are tested against real git repos.
**Demo:** After this: TBD

## Tasks
- [x] **T01: Added WorktreeManager in internal/auto/worktree.go with full git worktree lifecycle (Create/Merge/Remove/Exists) and WorktreeMode config field in Options** — 
